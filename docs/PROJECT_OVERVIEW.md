# 项目说明图

## 生产流程

![数字人工作台生产流程](./images/workflow.svg)

工作台把素材预检、旁白、15 秒预览、审批和最终成片串成一条有门禁的状态机。Agent 可以读取当前状态，但不能绕过用户审批直接提交最终生成。

## 系统架构

![数字人工作台架构](./images/architecture.svg)

前端负责项目操作和可视化状态；Agent 通过标准 MCP stdio 读取 Skill、调用工具和投递任务；后端适配层负责环境变量、路径限制、付费确认和 MiniMax/HeyGen 请求。
